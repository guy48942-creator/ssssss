# ANAS AI Voice & Context Upgrade

تمت إضافة مساعد AI عالمي للنظام يجمع بين:
- محادثة نصية مع Gemini عبر `/api/ai-chat`.
- التعرف على الصوت من المتصفح عبر Web Speech API.
- تحويل رد الذكاء الاصطناعي إلى صوت عبر SpeechSynthesis.
- سياق حي للنظام: عدد الحسابات، العملاء، الموردين، القيود، الفواتير، الأرصدة، آخر العمليات، السمة، العملة الرئيسية، وصلاحيات المستخدم.
- فهم بنية أقسام النظام مع تنقل آمن إلى القسم المطلوب.
- زر مساعد عائم متوافق مع الهاتف والوضع الليلي.
- لا يتم وضع مفتاح Gemini داخل الواجهة؛ يبقى في الخادم عبر `GEMINI_API_KEY`.
- لا ينفذ المساعد تعديلات مالية أو حذفاً من خلال المحادثة؛ التنقل والإرشاد فقط في هذه المرحلة.

## متطلبات التشغيل
ضع:
`GEMINI_API_KEY=...`

اختياري:
`GEMINI_MODEL=gemini-3.7-flash`

## الصوت
يحتاج المتصفح إلى دعم Speech Recognition. على Android يفضّل Chrome مع السماح بالميكروفون.
